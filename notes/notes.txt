RESTART PSQL:
sudo service postgresql restart
bam5716*

KILL TERMINALS:
kill -9 $(pgrep bash)

MYSQL:
npx dotenv sequelize db:migrate
npx dotenv sequelize db:seed:all

HEROKU:
heroku run npm run sequelize-cli db:migrate
heroku run npm run sequelize-cli db:seed:all

npx sequelize model:generate --name Category --attributes type:string;
npx sequelize model:generate --name Restaurant --attributes ownerId:integer,categoryId:integer,title:string,description:text,address:string,state:string,city:string,zipCode:integer,ratingOverall:float,ratingFood:float,ratingService:float,ratingAmbience:float;
npx sequelize model:generate --name Photo --attributes restaurantId:integer,imgUrl:string;
npx sequelize model:generate --name MenuItem --attributes restaurantId:integer,food:string,price:float;
npx sequelize model:generate --name FavRestaurant --attributes userId:integer,restaurantId:integer;
npx sequelize model:generate --name Reservation --attributes userId:integer,restaurantId:integer,time:date,numPpl:integer,specialReq:string;
npx sequelize model:generate --name Review --attributes userId:integer,restaurantId:integer,comment:text,ratingOverall:float,ratingFood:float,ratingService:float,ratingAmbience:float;

FRONTEND:
npm install react-datepicker --save
